# Chess
    TypeScript + Vue 3 + Vue Router + Vuex

### Менеджер пакетов
    yarn

## Старт проекта
    -- Установка зависимостей --
    yarn install

    -- Запуск сервера для разработки --
    yarn dev

    -- Сборка проекта --
    yarn build
